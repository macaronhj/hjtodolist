package first.project.todolist.login.service;

import first.project.todolist.login.dto.LoginDto;
import first.project.todolist.login.mapper.LoginMapper;

public class LoginService {
    public String requestLogin(LoginDto loginDto) {
        String requestLogin = "failed to login";
        return requestLogin;
    }
}
